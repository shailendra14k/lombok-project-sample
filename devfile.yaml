schemaVersion: 2.2.2
metadata:
  name: java-lombok
parent:
  uri: https://raw.githubusercontent.com/shailendra14k/devspace/refs/heads/main/devfiles/base/devfile-java.yaml
commands:
  - id: build
    exec:
      component: tools
      workingDir: ${PROJECTS_ROOT}/lombok-project-sample
      commandLine: mvn clean install
      group:
        kind: build
        isDefault: true
